name: TEST notify-failure (throwaway)

# Throwaway E2E test of the keyless failure-alert wiring. Deliberately fails, then the
# notify-failure job (same logic as the real workflows) must open a GitHub issue. Delete
# this file once verified. workflow_dispatch only — never scheduled.
on:
  workflow_dispatch: {}

jobs:
  notify-failure:
    needs: [fail-on-purpose]
    if: failure()
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Open or update a failure issue
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          WF: ${{ github.workflow }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set -euo pipefail
          BODY="Workflow **${WF}** failed at $(date -u +%Y-%m-%dT%H:%MZ). Run: ${RUN_URL}"
          NUM=$(gh issue list --repo "$REPO" --state open --search "${WF} failed in:title" --json number --jq '.[0].number // empty')
          if [ -n "$NUM" ]; then
            gh issue comment "$NUM" --repo "$REPO" --body "$BODY"
          else
            gh issue create --repo "$REPO" --title "🔴 ${WF} failed" --body "$BODY"
          fi

  fail-on-purpose:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "deliberate failure to exercise the notify-failure alert path"
          exit 1
